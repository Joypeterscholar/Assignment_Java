import junit.framework.TestCase;
import org.junit.Assert;
import org.junit.Test;

public class MultiTest {
Multi multi = new Multi();
@Test
public void multiply(){
Multi multi = new Multi();
Assert.assertEquals(15, multi.negativeMullti(-5,-3));
        }
}